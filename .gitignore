# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local

# typescript
*.tsbuildinfo

# vercel
.vercel

# agent skills installed via `npx skills add` (GSAP). Kept on disk but out of
# the app PR pending a design-system decision — GSAP collides with the locked
# motion contract (docs/03_MOTION.md, docs/06_DO_NOT.md). Remove these lines to
# commit the skills once that decision is made.
/.agents/
/.claude/
/skills-lock.json
